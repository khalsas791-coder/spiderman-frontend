{
  "name": "spiderman-project",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd modern-admin/client && npm install && npm run build",
    "server": "cd modern-admin/server && npm install && npm start",
    "dev": "concurrently \"npm run server\" \"cd modern-admin/client && npm run dev\""
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
